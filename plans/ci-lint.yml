# CI Lint — GitHub Actions validation workflow
#
# Independent of other projects. Can be done anytime.

project: ci-lint
status: pending

pending:
  - bump: patch
    summary: "add GitHub Actions lint workflow"
    files:
      create: [.github/workflows/lint.yml]
    details: |
      Runs on ubuntu-latest (shellcheck via apt). Triggers on push to main + PRs.
      Runs ./scripts/validate.sh.
      Consider also running `stow -n` (dry-run) once stow-migration is done.
